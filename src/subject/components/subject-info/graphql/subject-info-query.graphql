query SubjectInfo($subjectCode: String) {
  subject(where: { code: $subjectCode }) {
    id
    code
    description
    permissions
    department {
      id
      name
    }
    teachers {
      fullName
      email
      profilePictureURL
    }
    informations {
      ...SubjectInfoData
      permissions
    }
  }
}

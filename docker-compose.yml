version: '3.5'

services:
  cogito-frontend:
    container_name: cogito-frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:80'
    environment:
      - NODE_ENV=production
